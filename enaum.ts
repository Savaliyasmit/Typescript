// enum  make enum using enum keyword
// enum not store numaric value
// he work like array
enum person{
     smit,milan,sandip,false
}
// let result = person.milan
let result = person.false

console.log(result);
